//Reset function
document.addEventListener("button-cancel").addEventListener("click",reset);
function reset (ev) {
    ev.prevenDefault();
}

//Send function
document.getElementById("button-send").addEventListener("click",send);
function send () {
    
}